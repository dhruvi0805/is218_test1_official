#PROJECT: Test 1

Name: Dhruvi Malusare

Assignment is to learn push, pull, issues, fork, set up and run pytest.

The requirements decribes what versions of dependencies/packages are used in the application. The requirements.txt doc is committed so that it stays in the repo with the rest of the code, so while working locally people can download appropriate versions

The .venv files are ignored as they are dependencies/packages not originally written by us. 